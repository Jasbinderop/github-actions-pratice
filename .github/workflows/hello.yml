---
name: Hello World workflow
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'feature-**'
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello World from GitHub Actions"
